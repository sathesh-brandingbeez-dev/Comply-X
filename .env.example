# SMTP configuration for Comply-X email service
# Copy this file to `.env` and update the values to match your SMTP provider.

# Hostname or IP address of the SMTP server
SMTP_SERVER=smtp.yourprovider.com

# Port for the SMTP server (587 for TLS, 465 for SSL, 25 for plain)
SMTP_PORT=587

# Username used to authenticate with the SMTP server
SMTP_USERNAME=your_smtp_username

# Password or app-specific password for the SMTP server
SMTP_PASSWORD=your_smtp_password

# Email address that appears in the "From" field
FROM_EMAIL=notifications@yourdomain.com

# Friendly name that accompanies the From email address (optional)
FROM_NAME=Comply-X Notifications
